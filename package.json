{
  "name": "multi-step-form",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "watch": "rollup -c rollup.config.js --watch",
    "server": "live-server",
    "dev": "yarn watch | yarn server",
    "build": "rollup -c rollup.build.js"
  },
  "devDependencies": {
    "@rollup/plugin-terser": "^0.4.3",
    "live-server": "^1.2.2",
    "rollup": "^3.28.1",
    "rollup-plugin-scss": "^4.0.0",
    "sass": "^1.66.1"
  }
}
